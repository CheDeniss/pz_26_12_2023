import React from 'react';

const DisplayEx4 = ({numFromButton}) => {

    return (
        <div style={{fontSize: "x-large"}}>
            {numFromButton}
        </div>
    );
};

export default DisplayEx4;