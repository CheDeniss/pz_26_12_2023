import React from 'react';

const DisplayEx3 = ({numFromButton}) => {
    return (
        <div style={{fontSize: "x-large"}}>
            {numFromButton}
        </div>
    );
};

export default DisplayEx3;
